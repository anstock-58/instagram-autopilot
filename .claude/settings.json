{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd 'C:\\Users\\andre\\claude-workspace-vorlage' && git add -u && git diff --cached --quiet || (git commit -m \"Auto-Shutdown: $(date +%Y-%m-%d)\" && git push origin master)"
          }
        ]
      }
    ]
  }
}
